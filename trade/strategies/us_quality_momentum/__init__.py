"""US Quality Momentum strategy package (B025).

Subpackages added incrementally per feature:

- ``ranking`` (F002) — rank-based standardization helpers.
- ``factors`` (F002) — 5 pure-function factor calculations.

Parameters, construction, and signal-generation modules arrive in F003/F004.
"""
