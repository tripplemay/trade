# B111 — P0 生产修复 + A 股股票层低波 first-look

**日期：** 2026-07-21
**前置：** B110（`done`，最终裁定 NO-GO）
**性质：** 双工作流。**A = 生产代码修复**（触碰 `trade/`，需 CI + 独立验收）；
**B = research-only first-look**（零 API 成本，0 行生产代码）。

---

## 0. 这个批次为什么长这样

**★用户 2026-07-21 三项裁定：**
1. **A + B 并行**（两个不同性质的工作流，不抢资源）；
2. 低波用**双判据**：波动率声明为主 + alpha 强制并排披露；
3. **新信号搜索正式冻结**，重开条件限定为「数据类别变了」（已写入 scout SOP §禁区）。

**开这个批次的直接原因（git 已核实）：**
`docs/research/us-hk-strategy-diagnosis-2026-07-20.md`（commit `c0aa27f`，07-20 10:27）
复现了三个 P0，第 165 行明写「批次 A（修复批）… **建议立即**」。
实际发生的是：**同日开的下一个批次是 B110——又一次信号搜索**，一天后 NO-GO；
`git log --since=2026-07-19` 对四个受影响路径 **零次修复提交**。

> ★**这是一个需要被记住的模式**：连续四次信号研究为负期间，
> **没有一个批次用于检查已部署的东西**。本批次的工作流 A 就是打破它。

---

## 工作流 A — P0 生产修复（恢复「被验证过的设计」）

### A.0 边界

- **触碰 `trade/` 产品代码**，因此：每个 feature 必须有回归测试、必须过 CI、必须独立验收。
- **不引入新策略、不改任何策略的设计意图**。目标是让**跑着的东西 = 被验证过的那个设计**，
  不是让它变得更好。任何「顺手优化」都属越界。
- 修复后 Master 会真正持仓 → **实盘成本会上升**（诊断实测 5 周 9 次调仓 $419 ≈ 0.42% NAV，
  年化约 5%）。F004 的最小交易阈值必须同批落地，否则修完是把一个 bug 换成另一个问题。

### A.1 三个 P0（诊断 §2-§4 已端到端复现）

| # | 缺陷 | 影响仓位 | 现状（本 spec 起草时在 HEAD 逐行核实仍在） |
|---|---|---|---|
| **P0-1** | momentum sleeve「月线参数 × 日线数据」 | **40%** | `global_etf_momentum.py` 的 `momentum_windows` = `periods=3/6/9` 按**月线**设计，生产喂**日线** → 实际是 3/6/9 **天**动量；且候选池未按 ETF 白名单过滤，27 只个股混入，实盘持 CAT / HD |
| **P0-2** | us_quality sleeve 被 A 股日历静默毒死 | **20%** | A 股行情并入统一 CSV 后，中美交易日历**并集**在滚动窗口打出 NaN 空洞（`factors.py` 的 `min_periods=window`）→ `low_vol`/`trend` 全 NaN → 复合分全空 → **0 持仓回落 SGOV**，而元数据仍报 `scored`/`real` |
| **P0-3** | regime_adaptive 目标冻结 | 100%（该账户） | 目标权重停在 **2026-05-29**，7 周未动；6-12 手动刷新 4 连败（QQQ 缺历史），B107 迁移错过 7 月月度 timer |

★**P0-2 的性质最坏**：它不是「算错了」，是**算不出来却报告成算出来了**。
`sleeve_status` 报 `scored` 而实际 0 持仓，使监控完全看不见——这与 B109/B110 反复
遇到的「静默失败」是同一族问题（静默截断 / 静默空表 / 静默回落）。

---

## 工作流 B — A 股股票层低波 first-look

### B.0 ★★诚实性声明：本方向**不是**干净的预注册

**必须写在最前面**：低波的点估计**在写这份 spec 之前已经被看到了**
（2026-07-21 方向评估的席位产出）：

| | 几何年化 | 月度 σ | Sharpe | MaxDD |
|---|---:|---:|---:|---:|
| 等权基准 | 10.444% | 8.084% | 0.495 | −55.07% |
| **低波 V1** | **13.853%** | 6.873% | 0.666 | −45.95% |
| 高波 V5 | 2.431% | 9.807% | 0.238 | — |

五分位单调（13.85 / 13.47 / 12.46 / 9.23 / 2.43），超额 **+3.409pp**
（拆分：降波 +1.403pp、选股 +2.006pp；★**降波分量 11/11 年命中，选股分量 7/11 年**）。
月度算术超额 **t = 0.763**（NW(6) 0.888），bootstrap CI95 **[−0.86, +7.66] 含 0**，P(>0) = 94.9%。

**因此本批次的判据结构必须与 B110 不同：**

- 上表数字是**背景，不是证据**。它们**不得**作为「本方向成立」的依据被引用。
- **硬门放在两个尚未执行的证伪上**（见 B.2）——那里我们**真的不知道答案**，
  它们才是本批次唯一有信息量的部分。
- 任何阈值**不得由已观测值反推**。下文每个阈值都必须给出**独立于观测值的理由**；
  给不出理由的阈值不得写进判据。

> ★这一条是 B110 最贵的教训的直接应用：预注册只有在**看到结果之前**锁死才有意义。
> 本方向做不到，所以诚实的做法是**承认它做不到**，并把判断力转移到还没看过的地方——
> 而不是把已经看过的数字包装成预注册。

### B.1 冻结口径（计算规则，非判据）

| 项 | 规定 |
|---|---|
| 数据源 | `data/research/B110/ep_panel.csv.gz`（144 月，2013-01～2024-12）+ `data/research/B110/` 缓存。**零 API 成本** |
| 排序变量 | **过去 12 个自然月的月度收益已实现标准差**。★严格无前视：形成日 t 只用 t-12…t-1 的收益 |
| 分组 | 全市场五分位，等数量，V1 = σ 最低 |
| 基准 | 全市场**等权**（与 B110 的 B-scored 同构；同时出 B-wide 与二者之差） |
| 年化 | **几何** `∏(1+r)^(12/n) − 1`；★**算术版强制同表并排**（B110 D2 教训：signoff 裁定表未并排，掩盖了符号相反） |
| 收益 | 复用 B110 的 `fwd_ret_stub_0.00`（复权、含退市终值）。三档 stub 并排 |
| 可实施口径 | N=100 只、**半年调仓**（席位实测：净 13.39%、MaxDD −37.97%、年换手 80%、¥2.1 万/只） |

### B.2 ★★硬门：两个尚未执行的证伪（本批次的真正内容）

这两项**都还没做**，答案未知。**任一不过 → NO-GO。**

| 门 | 判据 | 阈值的独立理由（不得由观测值反推） |
|---|---|---|
| **G1 排序滞后** | 把 σ 排序**滞后一个月**（用 t-13…t-2 的收益排序，t 形成）后，几何超额仍 **≥ +1.0pp** | ★规模因子头条 **>50% 死在这一刀下**（lag0→1 断崖，Blume-Stambaugh 价格误差指纹）。σ 由过去收益算、与前向收益不重叠，**理论上不受同一偏误影响——但必须实测，不得推理**。阈值 1.0pp = B110 冻结的 NO-GO 线，沿用不新设 |
| **G2 流动性可执行** | 加日均成交额过滤（剔除最低 30% 流动性）后，几何超额仍 **≥ +1.0pp** | ★A 股低波溢价若住在流动性尾部，则对**任何**实盘规模都不可执行。30% 是执行侧惯例阈值，非拟合值。★**面板无成交量列 → 本门需补 `daily_basic.amount`，这是本批次唯一的 API 成本** |

### B.3 双判据（用户 2026-07-21 裁定的形态）

**G1/G2 双双通过后**，才进入下列判据：

| | 判据 | 阈值理由 |
|---|---|---|
| **主判据（风险声明）** | 低波组已实现 σ / 基准 σ **≤ 0.90**，且 **≥11/12 个独立年份**成立 | 0.90 = 「降波 10%」的设计要求，来自复利拖累 ≈ σ²/2 的量级需要；11/12 = 沿用 B110 的「分年 ≥60%」精神但对**风险声明**要求更高（风险声明若不能年年成立就不叫风险声明） |
| **副判据（复利声明）** | 几何超额 > 0 且 bootstrap **P(>0) ≥ 0.90** | 0.90 而非 0.95：本判据是副判据，且已声明不依赖均值显著性 |
| **★强制并排披露（不作判据）** | 月度算术超额及其 t / NW-t / bootstrap CI95 | ★**必须同表出现且明写「本方案不依赖它」**。已知 t = 0.763 < 2 —— **报告不得回避这一点**，也不得把它包装成通过 |

### B.4 ★H7 同款边界

- Generator **只算不裁**。产物与报告中不得出现 GO / NO-GO / 值得投入 / 有 edge 一类措辞。
- 裁定权归 Codex（工作流 B 的验收 feature）。
- 复用 B110 的机器判据：单测扫描产物 JSON 中的禁用词。

### B.5 ★随附的诚实限制（沿用 B110 §5，逐条仍成立）

无行业中性 / 无停牌涨跌停可执行性 / 无交易成本（G2 之外）/ 无公司行动处置。
★**追加两条本方向特有的**：

1. **数据层可信度分层**：B110 的**统计层**已被两路零-import 独立复算逐位证实；
   但**数据层**（PIT `total_mv` 的 PIT 性、复权/退市终值构造、宇宙幸存者偏差）
   **仍只有 B110 F004 单方证据，且其半数检查已被证明无鉴别力**
   （240/240 对拍是同义反复、漏斗闭合断言代数恒真）。本方向建立在同一份面板上，
   **继承该不确定性**。
2. **窗口敏感性**：B110 实测**窗口杠杆（10.89pp）大于数据质量杠杆（2.05~3.96pp）**——
   B110 的 NO-GO 若丢掉 2013 年会翻成 INCONCLUSIVE。本方向的窗口
   **在此冻结为 2013-01～2024-12 全区间**，分段结果须并排披露但不得用于挑选。

---

## 硬边界（两个工作流通用）

- **H1** 工作流 B 不改任何生产代码；工作流 A **只**改诊断 §6 列出的路径，不引入新策略、
  不改任何策略设计意图。
- **H2** 不产生可交易信号、不自动下单、不把任何 readiness 置 true。
- **H3** `DATA_NO_GO` 不因本批次改变。
- **H4** 覆盖不足结构化呈现，禁静默 dropna。★**P0-2 的教训升格为通用规则：
  算不出来时必须把状态标成 `fallback` 并告警，禁止报告成 `scored`。**
- **H5** 分页一律走 `scripts/research/ashare_pit/fetch.py`（G2 补成交额时适用），
  且必须带 B110 加的三道防线：行数下限 + 长退避 + **整页边界重取**。
- **H6** token 只从 `.env.local` 读。
- **H7** Generator 不下裁定（工作流 B）。

---

## Feature 拆分

| ID | executor | 工作流 | 内容 |
|---|---|---|---|
| **F001** | generator | A | P0-1：momentum sleeve 数据语义（月末重采样 或 窗口换算 63/126/189 交易日，按原设计文档语义二选一）+ **显式 ETF universe 白名单**。回归测试：日线输入下 3/6/9 月语义不变、个股不得进入候选池 |
| **F002** | generator | A | P0-2：因子管道先按 universe 过滤再 pivot + 滚动窗口对日历孔洞的显式策略（US 日历重索引）。★**空持仓回落时 `sleeve_status` 必须标 `fallback` 并告警，禁止报 `scored`**。回归测试：混入 A 股行情后 `low_vol`/`trend` 非 NaN 数 > 0 |
| **F003** | generator | A | P0-3 + 监控：补跑 regime precompute；月度 timer 加 `Persistent=true` 校验 + 失败告警；危机检测改**每日评估**（调仓仍月度）。B080 指标覆盖 master/regime；`as_of_date` 超 45 天未前进 → 告警 |
| **F004** | generator | A | 成本对齐：paper 与回测统一执行/成本假设并**量化二者差距**（★席位指出回测系统性乐观：回测假设 1bp+2bp 单边，paper 收 5+5bps，信号 T→T+1 open vs paper 当日收盘）；调仓加最小交易阈值，杜绝 $17 级碎单 |
| **F005** | generator | B | 低波 first-look 计算工具 + **G1/G2 两个硬门的执行**。★只算不裁（H7）。G2 需补 `daily_basic.amount`，走 `fetch.py` 三道防线 |
| **F006** | codex | A | 独立验收工作流 A：★**不得只看单测**，须核验修复后的实际持仓/因子非空数/regime as_of_date 前进/告警可触发。★成本上升是预期内的，须量化而非当缺陷 |
| **F007** | codex | B | 独立验收工作流 B + **下裁定**。★须自选样本独立复算；★**核查 B.0 的诚实性声明是否被遵守**——即报告有没有把已看过的数字当成预注册证据；★核查算术 t 是否被并排披露而非回避 |

---

## ★给 Codex（F006/F007）的前置提醒

B110 F004 的验收暴露了三处**证据强度**问题，本批次不要重犯：

1. **抽样池不得按构造排除被检项**（B110 的「240/240 对拍 MATCH」是同义反复：
   抽样池 `ep` 非空 ⟺ R2 已全 MATCH，BREAK 按构造抽不到）。
   ★**任何「一致率/通过率」必须同时报告分母是怎么筛出来的。**
2. **禁恒真断言**（B110 的漏斗闭合断言 `(a−b)+(b−c)+(c−d)+d ≡ a` 望远镜相消，
   捏造数字也返回 True）。
3. **驱动裁定的输入必须有独立留痕**（B110 的三档 stub 敏感带无 F004 独立重算，
   而它恰是驱动 D6 的输入；且事后核实发现该带只作用于 **212 个观测 = 样本的 0.04%**）。
4. ★**逐条核对冻结条款并在报告中列出已核对的编号**（B110 signoff 漏读 D8，
   却把漏读记成「框架缺少优先级定义」）。
