# B113 Blocker 2026-08-08

> 状态：**FAIL / 退回 fixing**
> 触发：F003 Codex 独立验收发现 F001 生产运行脚本未包含 B113 cleanup 修复。

## Scope

- F001 OPS1：`workbench-backup` cleanup 鲁棒性，本地 L1 + 生产只读/临时文件核验。
- F002 PRB1：`partial_rebalance` A/B artifact 独立复算与预注册裁定。
- F003：Codex 独立验收与裁定。

## Result

- F001：**FAIL**。仓库文件和本地 L1 PASS，但生产 systemd 实际执行的 `/opt/workbench/workbench-backup.sh` 仍是旧 cleanup 逻辑；B113 修复未到达真实 backup service。
- F002：**PASS**。artifact 结构、冻结窗口、delta、年化换手倍率和 H7 no-verdict 边界通过独立 verifier；裁定见下。
- F003：**BLOCKED**。因 F001 high finding，不能输出 signoff，不能置 `done`。

## Evidence

### F001 local L1 PASS

```text
$ bash -n workbench/deploy/backup/workbench-backup.sh
$ bash scripts/test/b113_backup_cleanup_test.sh
PASS: 清理删除本 run 暂存文件且返回 0
PASS: 空通配返回 0
PASS: 通配位被不可删对象占位时清理返回 0 且不误删
全部用例通过
```

仓库 HEAD 的修复点：

```text
workbench/deploy/backup/workbench-backup.sh:62  rm -f "${STAGE_FILE}" "${STAGE_GZ}" 2>/dev/null || true
workbench/deploy/backup/workbench-backup.sh:64  for f in /tmp/wb-*.db /tmp/wb-*.db.gz; do
workbench/deploy/backup/workbench-backup.sh:66    if [[ -O "$f" ]]; then
workbench/deploy/backup/workbench-backup.sh:67      rm -f "$f" 2>/dev/null || true
```

### F001 production runtime mismatch

Production service:

```text
$ ssh deploysvr 'systemctl cat workbench-backup.service | sed -n "1,120p"'
User=deploy
Group=deploy
ExecStart=/opt/workbench/workbench-backup.sh
```

Deployed runtime script still old:

```text
$ ssh deploysvr 'nl -ba /opt/workbench/workbench-backup.sh | sed -n "54,57p"'
54 cleanup_stage() {
55   rm -f /tmp/wb-*.db /tmp/wb-*.db.gz
56 }
57 trap cleanup_stage EXIT
```

Hash mismatch:

```text
production /opt/workbench/workbench-backup.sh:
0f72137962d27e6a6b627d4c401230d804a9fa6ccaef1915b4ad6a7475b66a04

repo workbench/deploy/backup/workbench-backup.sh:
1cce1080855464a59605ce07cab8645eed8a80532b06e870c1304afa9dbf325b
```

Root-owned marker test against production runtime:

```text
$ ssh deploysvr '<create root-owned /tmp/wb-b113-codex-other-owner-*.db; run cleanup_stage as deploy>'
rm: cannot remove '/tmp/wb-b113-codex-other-owner-1048202.db': Operation not permitted
```

Side effect was restored:

```text
$ ssh deploysvr 'unlink /tmp/wb-b113-codex-other-owner-1048202.db 2>/dev/null || true; test ! -e ... && echo cleaned'
cleaned
```

### F002 independent artifact verifier PASS

```text
$ .venv/bin/python scripts/test/b113_verify_partial_rebalance_artifact.py
pure_momentum__100000: capacity NO-GO (ΔCAGR=-0.110331pp, ΔMaxDD=-1.674967pp, turnover_ratio=0.920901, passes={'cagr': False, 'maxdd': True, 'turnover': True})
pure_momentum__1000000: mechanism NO-GO (ΔCAGR=+0.261999pp, ΔMaxDD=-1.434671pp, turnover_ratio=0.990713, passes={'cagr': False, 'maxdd': True, 'turnover': True})
quality_momentum__100000: capacity GO (ΔCAGR=+5.031585pp, ΔMaxDD=+8.462755pp, turnover_ratio=0.874697, passes={'cagr': True, 'maxdd': True, 'turnover': True})
quality_momentum__1000000: mechanism NO-GO (ΔCAGR=+0.807454pp, ΔMaxDD=-0.239615pp, turnover_ratio=1.194419, passes={'cagr': False, 'maxdd': True, 'turnover': True})
PASS: B113 F003 artifact checks and adjudication match pre-registered criteria
```

Other F002 checks:

```text
$ .venv/bin/python -m pytest tests/unit/test_b113_partial_rebalance_ab.py -q
4 passed in 0.27s

$ .venv/bin/python -m scripts.research.b113_partial_rebalance_report --out /tmp/b113-render-check.md
$ cmp -s docs/research/B113-F002-partial-rebalance-ab.md /tmp/b113-render-check.md
# exit 0
```

CI evidence:

```text
16bc82c Python CI: success (run 31144843737)
16bc82c Workbench Backend CI: success (run 31144843723)
16bc82c Workbench Deploy: success (run 31145291955)
```

Production `/api/health`:

```json
{
  "status": "ok",
  "version": "16bc82c8b1e90b895554a332583d858a11895b1b",
  "db_connectivity": "ok",
  "last_backup_age_seconds": 20179.735,
  "last_backup_size_bytes": 14275798
}
```

`16bc82c..HEAD` only contains non-runtime drift:

```text
docs/research/B113-F002-partial-rebalance-ab.json
docs/research/B113-F002-partial-rebalance-ab.md
features.json
progress.json
```

## Finding

### B113-F001-1

- **Severity：** high
- **Finding：** F001 修改了仓库内 `workbench/deploy/backup/workbench-backup.sh`，但生产 `workbench-backup.service` 实际执行 `/opt/workbench/workbench-backup.sh`；自动 deploy 后 `/opt` 脚本仍为旧通配清理，B113 OPS1 未在真实 service 生效。
- **Evidence：** 见上方 `ExecStart=/opt/workbench/workbench-backup.sh` 与旧 `cleanup_stage()` 片段；root-owned marker 复现 `Operation not permitted`。
- **Required Fix：** Generator 需让真实运行路径获得 B113 修复：更新 `/opt/workbench/workbench-backup.sh` 的安装/部署机制，或调整 service/runbook 使 `ExecStart` 指向随 release 更新且包含修复的脚本。修复后必须重新运行 root-owned `/tmp/wb-*` marker 验证，并证明测试 side effect 已清理。

## PRB1裁定（F002 已通过，但不签收整批）

| mode | 10万容量档 | 100万机制档 | 说明 |
|---|---|---|---|
| pure_momentum | **NO-GO** | **NO-GO** | 两档主判据 OOS ΔCAGR 均 < +1.0pp |
| quality_momentum | **GO** | **NO-GO** | 10万三项全过；100万主判据 +0.807pp < +1.0pp |

## Required Action

1. Generator 修复 F001 production runtime mismatch。
2. 修复完成后切 `reverifying`，Codex 复验：
   - 本地 `bash scripts/test/b113_backup_cleanup_test.sh`
   - 生产 root-owned `/tmp/wb-*` marker 跳过验证
   - `/opt/workbench/workbench-backup.sh` 与仓库修复逻辑/哈希对齐
3. F002 无需重算，除非修复改动触碰 PRB1 数据链或报告。

## Conclusion

B113 **不得 sign off**，不得置 `done`。当前应退回 `fixing`。
