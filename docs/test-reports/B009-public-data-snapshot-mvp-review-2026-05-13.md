# B009 Public Data Snapshot MVP Review 2026-05-13

## Scope

Evaluator performed F006 independent L1 verification for B009 Public Data Snapshot MVP.

Reviewed areas:
- B009 spec and acceptance criteria.
- Manual public data import boundary.
- Local snapshot manifest generation.
- Explicit snapshot loader integration.
- Data quality and research limitation labels for imported snapshots.
- Reproducible research run artifact structure.
- Fixture-first defaults and no-live/no-secret/no-network-by-default/no-broker/no-AI safety guards.

## Result

FAIL. One blocking acceptance gap remains.

## Findings

| ID | Severity | Finding | Evidence | Required Fix |
|---|---|---|---|---|
| B009-F006-1 | high | Snapshot manifests are generated by public import, but backtest/research-run artifacts do not reference the generated manifest file or manifest `snapshot_id`. This violates B009 Acceptance Summary: "Snapshot manifests are generated and referenced by research/backtest artifacts." | `trade/data/public_import.py` writes `*-manifest.json`, but `trade/reporting/reports.py` `build_research_run_artifact()` only records `data_snapshot_id`, `snapshot_kind`, `checksum`, `source`, and `date_range`. Explicit snapshot workflow evidence under `/tmp/opencode/b009-f006/run/b009-explicit-snapshot-research-run.json` contains no manifest path or manifest snapshot ID. | Add a manifest reference to the snapshot/backtest artifact path. Acceptable options: load/propagate manifest metadata into `DataSnapshot` and reports, or add an explicit workflow/report config field that records `manifest_file` and manifest `snapshot_id` in JSON report and research-run artifact. Add tests proving imported snapshot reports/artifacts reference the manifest. |

## Passing Evidence

Commands executed:

```text
.venv/bin/python -m pytest
59 passed in 1.18s

.venv/bin/python -m ruff check .
All checks passed!

.venv/bin/python -m compileall trade tests
PASS

.venv/bin/python -m mypy --install-types --non-interactive trade
Success: no issues found in 22 source files
```

Manual/default boundary evidence:
- `import_public_data_stub()` fails closed unless an explicit manual request is made.
- `PublicImportRequest.manual_confirmation` is required.
- CLI exposes `--i-understand-this-is-manual-research-data`.
- Import implementation copies local files only; no network modules are imported by `trade` package.
- Output path is constrained under gitignored `data/`.
- `.gitignore` ignores `data/*` except `data/.gitkeep`.

Explicit snapshot loader evidence:

```text
Attempted explicit snapshot path: /tmp/opencode/b009-f006/source/manual-public.json
Result: FixtureDataError("snapshot file must be an explicit local data/ or tests/ path")

Attempted gitignored snapshot with missing defensive asset:
Result: MomentumSignalError("defensive asset must exist in price records")

Attempted gitignored snapshot with insufficient history:
Result: MomentumSignalError("not enough history for SPY")
```

These are acceptable fail-closed behaviors.

Positive explicit snapshot workflow evidence:

```text
/tmp/opencode/b009-f006/run/b009-explicit-snapshot.json
/tmp/opencode/b009-f006/run/b009-explicit-snapshot.md
/tmp/opencode/b009-f006/run/b009-explicit-snapshot-research-run.json

data_snapshot_id = snapshot:0bbbebcb6ad16cb6
snapshot_kind = imported_public_research_snapshot
environment = local_or_ci_fixture
research_limitations = (
  sample_data_source:manual-public-data-import,
  synthetic_fixture_data:not_investment_advice:not_live_trading_ready,
  not_point_in_time_production_data,
  optional_public_best_effort_non_pit,
  imported_snapshot_data,
  public-best-effort,
  non-PIT,
  research-only,
  not-live-trading-ready
)
research_run.mode = human-review-only
research_run.broker_free = True
```

## Acceptance Assessment

| Feature | Result | Notes |
|---|---|---|
| F001 Public data import CLI boundary | PASS | Manual confirmation required, default stub fails closed, no credentials/network imports, output constrained to gitignored `data/`. |
| F002 Local data snapshot manifest | PARTIAL | Manifest generation records provider/source, timestamp, tickers, date range, row count, file hash/path, and limitation labels. Blocking gap: downstream artifacts do not reference the manifest. |
| F003 Historical data loader integration | PASS | Default workflow uses committed fixtures; explicit local `data/` snapshot path works; missing/non-local/incomplete snapshots fail closed without fallback. |
| F004 Imported data quality gate and report labels | PASS | Imported snapshots expose `imported_snapshot_data`, public-best-effort, non-PIT, research-only, not-live-trading-ready labels and quality flags in reports. |
| F005 Reproducible research run artifact | PARTIAL | Artifact links strategy config, snapshot data ID/checksum/date range, quality summary, reports, and target weights. Blocking gap: no snapshot manifest reference. |
| F006 Independent evaluation | FAIL | This review records one blocking acceptance gap; no signoff issued. |

## Non-Blocking Notes

| ID | Note | Risk | Follow-up |
|---|---|---|---|
| N1 | Report `run.environment` remains `local_or_ci_fixture` even for explicit imported snapshots. | low | Consider future wording such as `local_or_ci_research` if snapshot workflows become user-facing; current `snapshot_kind` distinguishes imported snapshots. |
| N2 | Imported snapshot reports also retain the B008 synthetic fixture limitation string when source is `manual-public-data-import`. | low | Consider separating synthetic fixture limitation from imported public snapshot limitations in a later cleanup if this becomes confusing. |

## Conclusion

B009 should move to fixing. Do not sign off until generated snapshot manifests are referenced by backtest/research-run artifacts and covered by tests.
