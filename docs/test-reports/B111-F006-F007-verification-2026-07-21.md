# B111 F006/F007 首轮验收 2026-07-21

> 状态：**FAIL**
>
> 触发：`verifying` 首轮验收；F001-F005 已交付，Codex 执行 F006/F007。
>
> 工作流 B 当前裁定：**NO-GO**。这不是实现异常，而是冻结双判据未通过。

## Scope

本轮覆盖两条工作流：

- 工作流 A：Momentum 月度 ETF 语义、US Quality 日历隔离与 fallback 诚实性、
  Regime 目标推进与 45 天 staleness 告警、paper 最小交易阈值、成本口径。
- 工作流 B：从 B110 面板独立重建 12 月已实现波动率排序、五分位收益、三档
  退市 stub、G1、G2、风险与复利双判据，并审查 B.0/B.1/B.5/H7。

没有执行生产数据库写入、外部通知、broker/订单、付费 API 或 L2 写操作。
生产证据全部来自只读健康检查、只读 SQLite、journal 和当前 release 的只读评分函数。

## Documents And Artifacts

- `docs/specs/B111-p0-fixes-and-lowvol-first-look-spec.md`
- `docs/research/us-hk-strategy-diagnosis-2026-07-20.md`
- `docs/research/B111-F004-cost-caliber-reconciliation.md`
- `docs/audits/B111-F005-low-vol-first-look.{json,md}`
- `docs/test-reports/B110-F004-adjudication-correction-2026-07-21.md`
- `scripts/test/b111_f007_verify.py`
- `docs/test-reports/B111-F007-evidence-2026-07-21.json`

## Verification

### Git, CI And Runtime Equivalence

- 验收前执行 `git pull --ff-only origin main`：`Already up to date`。
- 验收 HEAD：`279de7a003edfda39046c6bc0437c30a1184eaae`。
- Production `/api/health`：HTTP 200，version 与 HEAD 完整 SHA 相同，DB `ok`，
  backup `good`，active users `0`。
- CI：Python CI `29890004531`、Backend CI `29890004467`、Deploy
  `29890389012` 均为 `success`。
- `deploysvr` 当前 release：
  `/srv/workbench/releases/279de7a003edfda39046c6bc0437c30a1184eaae`。

### L1 Local

按唯一脚本启动，显式覆盖端口以遵守 `AGENTS.md`：

```bash
WORKBENCH_BACKEND_PORT=3099 WORKBENCH_FRONTEND_PORT=3100 \
  bash scripts/test/codex-setup.sh
WORKBENCH_BACKEND_PORT=3099 WORKBENCH_FRONTEND_PORT=3100 \
  bash scripts/test/codex-wait.sh
```

结果：backend 3099 HTTP 200，frontend 3100 HTTP 307，服务就绪。

```bash
.venv/bin/pytest -q \
  tests/unit/test_momentum_universe_and_frequency.py \
  tests/unit/test_us_quality_calendar_isolation.py \
  tests/unit/test_cost_reconciliation.py \
  tests/unit/test_ashare_pit_low_vol.py
```

结果：`30 passed`。

```bash
cd workbench/backend
../../.venv/bin/pytest -q \
  tests/unit/test_f003_regime_monitoring.py \
  tests/unit/test_paper_min_trade.py \
  tests/unit/test_recommendations_data_source.py \
  tests/unit/test_recommendations_precompute.py \
  tests/unit/test_data_refresh.py \
  tests/safety/test_market_scheduler_scope.py
```

结果：`150 passed`。其中构造的 all-NaN/pure-defensive 场景确认
`sleeve_status=fallback`、产生 warning、全局 `data_source` 不再报 `real`；
staleness 46 天触发，45 天边界不触发。

### F006 Production Read-Only Evidence

生产定时器和 journal：

- recommendations 最近一次：2026-07-22 03:00 CEST，成功，7 rows；但该运行早于
  HEAD 部署完成时间 06:14 CEST，因此数据库仍是旧实现快照。
- paper MTM 最近一次：03:45 CEST，也早于 F004/HEAD 部署。
- regime precompute：05:09 CEST 成功，`as_of_date=2026-06-30`，已从旧值
  `2026-05-29` 前进。
- timers 均为 active；regime timer 是每日 04:15，`Persistent=true`。

当前已发布但由旧代码产生的 Master 快照仍含 `CAT/HD` momentum 与
`SGOV=0.438127`，所以不能把它当作新代码失败，也不能把它当作新代码实际持仓通过。

为隔离部署时间边界，在当前 release、生产统一 CSV 上直接执行与 timer 相同的
`score_master_target()`，但不调用保存或 rationale：

- signal date：`2026-06-30`。
- Momentum：`EEM=0.2`、`QQQ=0.2`，仅 ETF；`CAT/HD` 不再属于 Momentum。
- US Quality：五个因子 `momentum/quality/low_vol/value/trend` 均为
  `27/27` 非 NaN；目标包含 15 个等权股票，每只约 `0.013333`，真实持仓目标大于 0。
- `CAT` 仍可由 US Quality 合法入选，归因是 `satellite_us_quality`，不是 Momentum。
- HK China 全防守被诚实标为 `fallback`，全局 `data_source=mixed`，证明 H4 通用规则生效。

生产 Master paper 成本只读汇总：

- 激活日之后 9 次调仓，合计 `$429.7968`，均次 `$47.7552`，占初始 NAV
  `0.4298%`，与诊断的 `$419/9 次` 同量级。
- 含激活构建共 14 条记录，合计 `$529.7267`。
- F004 2026-07-22 03:09:43 UTC 部署后调仓数为 **0**，因此当前没有最小交易阈值
  上线后的前后对比样本。
- shipped 参数仍是 backtest `1+2bps`、paper `5+5bps`；月度回测单边、paper
  双边，量化差为 `6.67x`，执行时点仍为 T+1 open 对当日 close。

### F007 Independent Recompute

```bash
python3 scripts/test/b111_f007_verify.py
.venv/bin/ruff check scripts/test/b111_f007_verify.py
```

结果：独立脚本不导入 `low_vol.py`、`low_vol_cli.py` 或 `signal_stats.py`；Ruff
`All checks passed`。脚本从 raw panel 每年按独立 seed 选 5 行，共 60 行，抽样不以
sigma、分组或 eligibility 为条件：44 行有完整 12 月窗口，16 行按规则不可评估，均留痕。
这避免了 B110 的构造性排除。

所有五个 variant 的 75 个交付汇总字段与独立结果在 `1e-12` 容差内相等；逐月
分组收益、分组数、流动性可用数/剔除数、bootstrap 配对输入、三档 stub 全部写入
证据 JSON，没有使用漏斗望远镜恒等式。

| 口径 | 月数 | 几何超额 | sigma 比 | 低波年份 | bootstrap P>0 |
|---|---:|---:|---:|---:|---:|
| 主口径 stub 0 | 132 | +3.0859pp | 0.8573 | 11/11 | **0.8620** |
| 主口径 stub -0.30 | 132 | +3.1173pp | 0.8573 | 11/11 | 0.8650 |
| 主口径 stub -1.00 | 132 | +3.1906pp | 0.8573 | 11/11 | 0.8675 |
| G1 滞后一月 | 131 | +2.2098pp | 0.8513 | 11/11 | 0.7715 |
| G2 单日成交额代理 | 132 | +4.1056pp | 0.8429 | 11/11 | 0.9145 |

主口径算术年化超额 `+1.7954pp`，简单 t `0.7039`，NW(6) t `0.8154`，
分析 CI `[-1.9132pp, +8.0851pp]`。独立结果确认交付数字，但不改变冻结门槛。

## Frozen Clause Review

| 条款 | 结果 | 证据 |
|---|---|---|
| B.0 非干净预注册、已看数字只作背景 | PASS | JSON/Markdown 均有原样声明，未用已看点估计宣称预注册成立。 |
| B.1 数据源：B110 panel + B111 liquidity | PASS | 两个输入均直接读取并记录行数。 |
| B.1 t-12..t-1；G1 t-13..t-2 | PASS | 独立重算逐月匹配。 |
| B.1 五分位、V1 最低 sigma、等权 B-scored | PASS | 132/131 月结果匹配。 |
| B.1 B-wide 与 B-scored 差值 | **FAIL** | 交付只有 B-scored，没有 B-wide 或二者差。 |
| B.1 几何/算术年化 | PASS | JSON 两者均存在。 |
| B.1 stub 0/-0.30/-1.00 并排 | PASS | 三档独立持久化并匹配。 |
| B.1 可实施 N=100、半年调仓 | **FAIL** | 交付仅有全市场五分位、月度调仓。 |
| B.2 G1 >=+1.0pp | PASS | 独立值 +2.2098pp。 |
| B.2 G2 日均成交额剔除最低 30% | **FAIL** | 实现使用形成日单日 `daily.amount`，报告也承认“非滚动均值”；+4.1056pp 仅是代理值。 |
| B.3 风险 sigma 比 <=0.90 | PASS | 0.8573。 |
| B.3 至少 11/12 独立年份 | **FAIL** | burn-in 后只有 11 个可评估年份；11/11 不能证明冻结的 11/12 分母。 |
| B.3 几何超额>0 且 bootstrap P>=0.90 | **FAIL** | 几何为正，但主口径 P=0.862。 |
| B.3 算术超额、t/NW/CI 同表且不依赖 | **FAIL** | JSON 有数字与“不依赖”标记；Markdown 未把算术超额和 CI 与主表同表披露。 |
| B.5 全窗口与分段并排 | **FAIL** | 全窗口冻结正确，但没有分段结果并排。 |
| H7 Generator 不裁定 | PASS | 交付无 Generator verdict；裁定由本报告给出。 |

## Findings

### B111-F006-1

- **Severity:** high
- **Finding:** F004 没有完成冻结要求的 paper/回测执行与成本假设统一，只量化了仍然存在的差距。
- **Evidence:** `trade/backtest/monthly.py` 仍为 1+2bps；paper 仍为 5+5bps；单/双边和
  T+1 open/当日 close 均未统一。F004 报告明确写“未改任何回测/paper 成本参数”。
- **Required Fix:** 选择并落地一个统一的前向成本与执行口径，保留旧历史产物的 provenance；
  重新给出统一前、统一后的成本差和受影响回测范围。仅增加比较函数不满足该 acceptance。

### B111-F006-2

- **Severity:** high
- **Finding:** F006 要求的“实际已发布持仓”和 min-trade 上线后成本对比尚无生产样本。
- **Evidence:** recommendations/paper 最近运行均早于部署；DB 仍是旧目标；F004 部署后
  paper rebalance count=0。当前 release 的只读评分通过，但不是 persisted snapshot/paper holding。
- **Required Fix:** 修复轮部署后让正常 recommendations 与 paper MTM 周期各成功执行一次；
  复验必须读取 persisted snapshot、paper positions 和新 rebalance ledger，不用测试函数替代。

### B111-F007-1

- **Severity:** high
- **Finding:** G2 没有执行冻结的“日均成交额”门，只执行了形成日单日成交额代理。
- **Evidence:** `low_vol_liquidity_fetch.py` 调用 `pro.daily(fields=ts_code,amount)` 单日数据；
  F005 Markdown 明确写“单日（非滚动均值）”。
- **Required Fix:** 冻结并实现无前视的日均窗口，重跑 G2；不得把代理 +4.1056pp 当正式 G2 通过。

### B111-F007-2

- **Severity:** high
- **Finding:** B.1 的 B-wide、N=100/半年调仓和分段并排结果缺失。
- **Evidence:** F005 JSON/Markdown 只有 B-scored 五分位月频全窗口结果。
- **Required Fix:** 补齐冻结输出并由独立脚本复验；不得改阈值或用新增结果选择窗口。

### B111-F007-3

- **Severity:** medium
- **Finding:** 强制算术披露未按“同表”要求完成，且报告在 G1/G2 已执行后仍写“两个尚未执行的证伪”。
- **Evidence:** Markdown 主表无算术超额/CI；JSON `hard_gates.role` 与 G2 `status=executed` 自相矛盾。
- **Required Fix:** 主表并排列出算术超额、简单 t、NW-t、CI，并改为历史时态；保持 H7 不下裁定。

## Decision

代理 G1/G2 数字都高于 +1.0pp，但正式 G2 口径没有执行，因此不能写“两道正式硬门均通过”。
即使暂时接受单日代理，B.3 仍有两个独立失败：只有 11 个可评估年份，且主样本
bootstrap `0.862 < 0.90`。按用户冻结的双判据，工作流 B 当前裁定为 **NO-GO**。

该裁定只回答是否应继续投入这条低波 first-look，不是可交易信号、收益承诺或 readiness；
`DATA_NO_GO` 保持不变。缺失输出应为证据完整性修复，不得以调参方式追求翻案。

## Boundary Review

| 边界 | 结果 | 说明 |
|---|---|---|
| H1 仅修诊断 F1-F5、不改策略意图；B 不改生产 | PASS | 产品变更对应 F1-F5；Momentum 核心参数未改；F005 仅 research/test/data。 |
| H2 不产生交易/订单/readiness | PASS | 无 broker/order/validated 路径，未写生产。 |
| H3 `DATA_NO_GO` 不变 | PASS | diff 无 readiness 或 DATA verdict 变更。 |
| H4 缺口结构化、fallback 不伪报 scored | PASS | 生产 direct score 将 HK fallback 标为 mixed；all-NaN 测试覆盖 warning/fallback。 |
| H5 分页三防线 | PASS | G2 fetch 经 `_fetch_cached -> fetch_paged`，有 min rows、长退避、整页重取。 |
| H6 token 仅 `.env.local` | PASS | loader 路径符合；仓库/输出无 token。 |
| H7 Generator 不裁定 | PASS | F005 只算；裁定在本报告。 |

## Soft-Watch

| ID | 描述 | 风险等级 | 建议处置 |
|---|---|---|---|
| S1 | `AGENTS.md` 要求 Codex 端口 3099，`codex-setup.sh` 默认仍是 backend 8723/frontend 3000。 | medium | Generator 在修复轮统一脚本默认值与规则；本轮用环境变量遵守 3099。 |
| S2 | 本地 frontend 在无 auth secret 时仍返回 307，但日志记录 `MissingSecret`。 | low | UI 非本批次范围；后续 UI 验收提供测试 secret 或明确只验 backend。 |
| S3 | HK China 全防守现在被通用规则标为 fallback/mixed，诚实但会使 Master 永远不报 full real。 | medium | Planner 后续明确这是预期 dead sleeve 还是应单独处置，不在本修复批改变策略意图。 |

## Coverage Gaps

- 未执行付费 API、外部通知或生产写入；G2 数据沿用已交付缓存。
- 没有 F004 部署后的 paper 调仓，所以 min-trade 的真实节省量尚不可量化。
- 当前已发布 Master snapshot 是部署前计算；当前 release 只读评分不能替代持久化目标与 paper 持仓。

## Conclusion

F001-F003 的实现和当前 release 真数据评分通过；F004、F005 存在上述 high findings，
F006/F007 不可签收。状态机应从 `verifying` 转到 `fixing`，`docs.signoff` 保持 null；
修复后由 Codex 在 `reverifying` 复验。工作流 B 的当前研究裁定为 **NO-GO**。
