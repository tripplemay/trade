# B009 Public Data Snapshot MVP Reverification 2026-05-13

## Scope

Evaluator re-verified B009-F006-1 after fixing round 1.

Original finding:
- `B009-F006-1`: Snapshot manifests are generated by public import, but backtest/research-run artifacts do not reference the generated manifest file or manifest `snapshot_id`.

## Result

FAIL. The fix is incomplete.

## Findings

| ID | Severity | Finding | Evidence | Required Fix |
|---|---|---|---|---|
| B009-F006-1R | high | Regression tests use a manually named `provider-prices-manifest.json`, but real `import_public_data()` writes the manifest as `{snapshot_id}-manifest.json`. `load_snapshot_prices()` only searches `{snapshot_file_stem}-manifest.json`, so reports for real imported outputs still have `snapshot_manifest = None`. | Real import evidence: output file `data/public-cache/eval-provider-manual-download.json`, manifest file `data/public-cache/public:eval-provider:9aa9f876caec416a-manifest.json`; workflow JSON report and research-run artifact both printed `None` for manifest. | Align importer and loader manifest naming/reference semantics. Either write the manifest adjacent to the imported file as `{output_file.stem}-manifest.json`, teach loader to find the actual `{snapshot_id}-manifest.json`, or include a deterministic manifest pointer. Add an E2E regression that calls `import_public_data()` and then `run_fixture_workflow()` on `result.output_file`, asserting JSON and research-run artifacts reference `result.manifest_file` and `result.snapshot_id`. |

## Passing Checks

```text
.venv/bin/python -m pytest
61 passed in 1.20s

.venv/bin/python -m ruff check .
All checks passed!

.venv/bin/python -m compileall trade tests
PASS

.venv/bin/python -m mypy --install-types --non-interactive trade
Success: no issues found in 22 source files
```

## Reverification Evidence

Command scenario:
- Create a fixture-shaped manual public source file under `/tmp/opencode/b009-reverify/source/manual-download.json`.
- Call `import_public_data(PublicImportRequest(..., provider="Eval Provider", output_directory=Path("data/public-cache"), manual_confirmation=True))`.
- Run `run_fixture_workflow()` with `snapshot_path=Path("data/public-cache/eval-provider-manual-download.json")`.

Observed output:

```text
data/public-cache/eval-provider-manual-download.json
data/public-cache/public:eval-provider:9aa9f876caec416a-manifest.json
public:eval-provider:9aa9f876caec416a
/tmp/opencode/b009-reverify/run/b009-reverify-imported.json
/tmp/opencode/b009-reverify/run/b009-reverify-imported.md
/tmp/opencode/b009-reverify/run/b009-reverify-imported-research-run.json
report data.snapshot_manifest = None
research_run snapshot_reference.manifest = None
```

## Notes

The added unit/workflow tests prove that a manually created `{snapshot_file_stem}-manifest.json` can be loaded and reported. They do not prove the actual importer output path works end-to-end. The B009 acceptance requires imported snapshot manifests to be generated and referenced by artifacts, so the real import-to-workflow path remains blocking.

## Conclusion

B009 remains in `fixing`. No signoff issued.
