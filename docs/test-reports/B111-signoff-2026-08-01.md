# B111 Signoff 2026-08-01

> 状态：**PASS**
> 触发：F006-3 fix-round 1 复验完成

## Scope

- 本轮只复验上一轮唯一 blocker `B111-F006-3`：min-trade 跳过卖单后继续买入导致 paper cash 为负。
- 覆盖纯引擎现金可行性、服务层告警、负现金恢复路径、paper 相关回归、CI、生产部署与数据库留痕。
- F006-1/2 和 F007-1/2/3 沿用已执行证据：
  `docs/test-reports/B111-F006-F007-reverification-2026-08-01.md`。
- F007 的最终 `NO-GO` 是冻结研究判据的有效完成结果，不是实现失败。

## Verification

| 检查 | 命令 / 证据 | 结果 |
|---|---|---|
| finding 最小复现 + 修复回归 | `.venv/bin/python -m pytest workbench/backend/tests/unit/test_paper_min_trade.py -q` | PASS，10/10 |
| 服务层现金受限告警 | 新增 `test_align_warns_when_skipped_sells_force_buy_scale_down`，通过真实 DB-backed `align_to_current_target` 路径断言缩买、cash >= 0、WARNING | PASS |
| paper 广覆盖回归 | `.venv/bin/python -m pytest workbench/backend/tests/unit/test_paper*.py workbench/backend/tests/acceptance/test_b071_golden_backend_invariants.py -q` | PASS，79/79 |
| 静态门禁 | `.venv/bin/python -m ruff check .` | PASS |
| CI | GitHub Actions，产品 SHA `fa1c6fe` | Backend CI、Frontend CI、Deploy 均 success |
| 签收提交 CI | GitHub Actions，签收 SHA `55a5f5e` | Backend CI、Frontend CI、两次链式 Deploy 均 success |
| 生产健康 | `GET https://trade.guangai.ai/api/health` | HTTP 200，DB ok，version=`fa1c6fe` |
| 生产部署 | `readlink -f /srv/workbench/current` | `/srv/workbench/releases/fa1c6fe...` |
| 当前账户现金 | 生产 SQLite 只读查询 `paper_account` | 5/5 账户 cash >= 0；Master `332.248912` |
| 恢复账本 | 生产 SQLite 只读查询 `paper_rebalance` | 2026-08-01 cost=`0.351545`，created_at=`06:50:33` |
| 历史快照独立重放 | 读取 08-01 Master `cash=-18.944156`、positions、冻结 target，调用生产 SHA 的 `compute_rebalance`，不写 DB | 精确复现 cash=`332.248912`、cost=`0.351545`、traded=`351.544613`、22 positions |

## High-Level Findings

- **B111-F006-3 已关闭。** 被 min-trade 跳过的卖单不再为买单提供虚假资金；资金不足时买腿按同一比例缩放，`buy_scale_factor` 可观测。
- `new_cash < -1e-6` 会 fail loud；仅 `|cash| <= 1e-6` 的浮点尘埃允许归零，不存在用 clamp 掩盖超买。
- 已有负现金账户通过 dust-sell recovery path 恢复，独立重放与生产账本逐数字一致。
- 修复范围只涉及 paper engine/service/CLI 和测试，没有改变策略信号、目标权重或 broker/readiness 边界。

## Soft-Watch

| ID | 描述 | 风险等级 | 建议处置 |
|---|---|---|---|
| S1 | `paper_nav_history` 的 2026-08-01 行仍保留修复前 `cash=-18.944156`，当前权威账户值已为正；该行同时是事故审计证据。 | low | 保留历史记录；下一 MTM 日确认新行继续为非负。 |
| S2 | `workbench-backup.service` 08-01 在备份文件已成功生成后，因清理 `/tmp/wb-ro.db` 权限失败而显示 failed。与 B111 diff 无关，health 显示最近备份有效。 | medium | Planner done 阶段登记独立 ops 修复项，修正临时文件 ownership/cleanup。 |
| S3 | `workbench-advisor.service` 因 AIGC Gateway 503 耗尽重试；发生在旧部署 `ac1340f`，与 F006-3 无关。 | low | 按既有 timer 重试并在运维队列跟踪 gateway 可用性。 |

## Framework Learnings

- min-trade / drift-band 等交易过滤发生后，必须基于**实际执行**的卖腿重新验证现金可行性；基于过滤前目标册做成本准备金仍可能超买。
- 生产状态修复应同时保留“故障快照 + 恢复账本 + 纯函数重放”三类证据，避免只凭一次 CLI 输出签收。

## Production / HEAD Equivalence

- 复验时生产与产品实现 SHA 均为 `fa1c6fe`。
- 签收提交 `55a5f5e` 相对该 SHA 仅增加 evaluator 测试、报告和状态机产物，不改变已验证产品实现；自动部署后 production version=`55a5f5e`。
- 本报告的收尾修订仅涉及 `docs/`、`.auto-memory/` 和 `progress.json`，按 chore-only 等价规则不要求再次部署。

## Post-Signoff Deploy

- 本轮没有 evaluator 手工生产写操作；所有生产核验均为 health、systemd、SQLite 只读查询或纯函数重放。
- Generator 已完成的受控 align 是唯一恢复写操作，账本记录如上。
- 推送签收提交后，既有 CI/CD 自动部署 `55a5f5e` 成功；部署后 health HTTP 200、DB ok、Master cash=`332.248912`、负现金账户数=0。

## Conclusion

F006-3 修复、可观测性、回归和生产恢复全部 PASS；F001-F007 均可签收，B111 可置为 `done`。
