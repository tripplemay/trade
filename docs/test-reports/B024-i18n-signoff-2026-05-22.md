# B024 i18n Signoff 2026-05-22

> 状态：**PASS**
> 触发：F006 fix-round-1 后完成 L1 复验与 L2 真 VM 双 locale 验收

## Scope

- L1 本地复验：frontend `lint` / `typecheck` / `vitest` / `build` / `npm audit --omit=dev --audit-level=high`，backend `pytest` / `ruff` / `mypy`，本地 `cash<0` locale 422，Playwright 主链
- L2 真 VM 验收：`https://trade.guangai.ai` 上的默认中文、LocaleSwitcher 英中切换与持久、logout 后英文登录页、manual execution zh/en 双轮受控写入闭环、Markdown 双语文件落盘、`/api/debug/recent-errors`、systemd 状态、账户恢复
- 不含范围：broker SDK / 自动下单 / 多用户 / 外部支付或通知

## Verification

- L1 PASS
  - backend：`pytest tests -q` → `240 passed, 2 skipped`
  - backend：`ruff check .` / `mypy workbench_api tests` → PASS
  - frontend：`npm run lint` / `npm run typecheck` / `npm test` → `146 passed`
  - frontend：`npm run build` → PASS
  - frontend：`npm audit --omit=dev --audit-level=high` → exit `0`（仅 `moderate`）
  - frontend：`.next/static` grep backend host/port → no match
  - local API：`PUT /api/execution/account` with `cash=-1` → `{"detail":"现金不能为负数。"}`；`?locale=en` → `{"detail":"cash cannot be negative."}`
  - local Playwright：`protected-routes + home-loads + disclaimer-present` → `19 passed`
- L2 PASS
  - `GET https://trade.guangai.ai/api/health` → `status=ok`, `db_connectivity=ok`, `version=0176056c68ef6e7f2923b2c15bfadd96f898c519`
  - `git rev-parse HEAD` → `0176056c68ef6e7f2923b2c15bfadd96f898c519`
  - `systemctl status workbench-backend.service` / `workbench-frontend.service` → 两个服务均 `active (running)`；frontend 为 `Next.js 15.5.18`
  - `GET /api/auth/session` with minted Auth.js HS256 cookie → `tripplezhou@gmail.com`
  - Production UI locale：
    - 匿名 `/login` 默认 `工作台登录`，首访写 `NEXT_LOCALE=zh-CN`
    - authed `/` 默认中文，页面正文包含 `首页` / `退出登录`
    - 切换 `en` 后页面正文包含 `Home` / `Sign out`，`NEXT_LOCALE=en`
    - sign out 后落到 `/login`，正文为 `Workbench sign-in`
    - 写回 `NEXT_LOCALE=zh-CN` 后重新进入 `/`，正文恢复 `首页`
  - `GET /api/execution/account/latest` 初始为 `snap-84ac2e8b398a`, `cash=0`, `positions=[]`
  - `GET /api/recommendations/current` → `target_positions=[]`
  - `GET /api/execution/risk-panel` → `state=red`, defensive ticket available
  - locale 负向验收：
    - `PUT /api/execution/account` + `Accept-Language=zh-CN` + `cash=-1` → `{"detail":"现金不能为负数。"}`
    - `PUT /api/execution/account?locale=en` + 同 body → `{"detail":"cash cannot be negative."}`
  - zh-CN manual execution 闭环：
    - `PUT /api/execution/account` → `snap-c4db6b469a25`（`cash=900`, `SPY 1 @ 100`）
    - `POST /api/execution/tickets {"defensive":true}` → `tkt-20260522-aa938ba2`
    - `POST /api/execution/fills/csv` 2 行 generic CSV → `matched=true` ×2
    - `POST /api/execution/reconcile/tkt-20260522-aa938ba2` → `snap-61160198c1a5`, `already_reconciled=false`, `unmatched_lines=[]`
    - `GET /api/execution/journal-history?since=2026-05-22` → 票据 `status=executed`, `fill_count=2`, `avg_bps=0.0`
    - `GET /api/execution/slippage-analytics?window=3m` → `rolling_avg_bps=0.0`
  - en manual execution 闭环：
    - `PUT /api/execution/account?locale=en` → `snap-13bbc054e51f`（同样 seed）
    - `POST /api/execution/tickets?locale=en {"defensive":true}` → `tkt-20260522-83e11b17`
    - `POST /api/execution/fills/csv?locale=en` 2 行 generic CSV → `matched=true` ×2
    - `POST /api/execution/reconcile/tkt-20260522-83e11b17?locale=en` → `snap-773077ecb6bc`, `already_reconciled=false`, `unmatched_lines=[]`
    - `GET /api/execution/journal-history?since=2026-05-22` → 两张新票据均 `executed`
    - `GET /api/execution/slippage-analytics?window=3m` → `rolling_avg_bps=0.0`
  - Markdown 文件落盘双语证据：
    - `/var/lib/workbench/runs/2026-05-22/order-ticket-tkt-20260522-aa938ba2.md`
    - `/var/lib/workbench/runs/2026-05-22/order-ticket-tkt-20260522-83e11b17.md`
    - `sudo grep` 命中 `Trades to place / 待下达交易`、`After execution checklist / 执行后核对清单`、英文 disclaimer、中文 disclaimer
  - `GET /api/debug/recent-errors` after full flow → `{"count":0,"records":[]}`
  - 副作用恢复：`PUT /api/execution/account {"cash":0,"base_currency":"USD","positions":[]}` → `snap-0d0597e11327`；随后 `GET /api/execution/account/latest` 仍为 `cash=0`, `positions=[]`

## High-level Findings

- F006 首轮 blocker 已被精准修复：`cash<0` 现在走 locale-aware 422，而没有引入全局 Pydantic 422 翻译副作用。
- 生产 UI 与 API 的双 locale 行为都已实测通过：默认中文、英文切换、logout 后英文登录页、中文回切持久。
- zh / en 两轮 manual execution 真实写入闭环均通过，且 debug buffer 保持 0。

## Ops 副作用记录

| Agent | 阶段 | 操作摘要 | 副作用对齐 | 用户授权 |
|---|---|---|---|---|
| Codex evaluator | reverifying | 两次 `PUT /api/execution/account` 写入临时 seed snapshot（`cash=900`, `SPY 1 @ 100`） | L2 完成后以 `PUT /api/execution/account {"cash":0,"positions":[]}` 恢复为零仓零现金 | 用户本轮“开始”授权 L2 |
| Codex evaluator | reverifying | 两次 `POST /api/execution/tickets` + `POST /api/execution/fills/csv` + `POST /api/execution/reconcile/{ticket_id}` | 保留 2 张测试票据、4 行 fill、2 条 journal/slippage 记录作为验收证据；debug buffer 仍为 0 | 用户本轮“开始”授权 L2 |

## Production / HEAD 等价性

| 项 | 值 |
|---|---|
| Production version (from `/api/health.version`) | `0176056c68ef6e7f2923b2c15bfadd96f898c519` |
| Main HEAD (`git rev-parse HEAD`) | `0176056c68ef6e7f2923b2c15bfadd96f898c519` |
| Diff | `0 commits` |

## Soft-watch

| ID | 描述 | 风险等级 | 建议处置 |
|---|---|---|---|
| S1 | 当前 production `recommendations/current.target_positions=[]`，L2 无法覆盖普通 recommendation-driven ticket，只能走 `defensive=true` 路径。 | medium | 后续若要演练 normal path，先准备最小 target positions 数据，再补一次非 defensive 冒烟。 |
| S2 | defensive ticket 当前对 SGOV 生成 `Shares=1,000`、`Reference close=—`，L2 只能按 research-only 协议用最小 generic CSV 受控闭环，不能把这当作真实市场 sizing 逻辑背书。 | low | 若后续要收紧 operator 体验，可单独评估 defensive proxy 的 share sizing / reference-price 表达。 |

## Framework Learnings

- 候选 1：i18n 中文按钮禁词扩集应继续作为 safety regression 长期保留。
- 候选 2：`next-intl` + NextAuth middleware chain + locale cookie 持久模式已在生产 L2 验证可行。
- 候选 3：compliance / disclaimer 文案保持双语并存、而不是按 locale 分叉，可避免历史 Markdown diff 漂移。

## Conclusion

可以签收。
