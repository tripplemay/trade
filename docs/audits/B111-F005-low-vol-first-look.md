# B111 F005 — A 股股票层低波 first-look（★只算不裁 H7）

**日期：** 2026-07-21
**产物：** `docs/audits/B111-F005-low-vol-first-look.json`（原始统计）
**工具：** `scripts/research/ashare_pit/low_vol.py` / `low_vol_cli.py` / `low_vol_liquidity_fetch.py`
**数据：** `data/research/B110/ep_panel.csv.gz`（144 月）+ `data/research/B111/low_vol_liquidity.csv.gz`（G2，`daily.amount`）

---

## ★★H7 边界 + §B.0 诚实性声明

**本文件与 JSON 产物只含计算值，不含任何裁定。** 主判据（σ 比 ≤0.90 且 ≥11/12 年）、
副判据（几何超额>0 且 bootstrap P>0≥0.90）的**施加**，以及 G1/G2 两个硬门的**判读**，
全部归 F007 的 Codex（铁律 #4）。

**本方向不是干净的预注册**：下列点估计在起草 spec 前已被席位看到，**故为背景、不作证据**。
判据的信息量在 **G1（排序滞后一月）/ G2（流动性）两个尚未执行的证伪**上——那里答案未知。

---

## 计算值（无前视 σ = 过去 12 月月度收益已实现 σ；窗口 2013-01～2024-12）

| 口径 | 月数 | V1−基准 几何年化超额 | 已实现 σ 比 V1/基准 | 分年 V1 更低波 | bootstrap P(超额>0) |
|---|---:|---:|---:|---:|---:|
| **主口径**（无滞后） | 132 | **+3.086pp** | 0.857 | 11/11 | 0.862 |
| **★G1 硬门**（σ 排序滞后一月 t-13…t-2） | 131 | **+2.210pp** | — | — | — |
| **★G2 硬门**（剔除最低 30% 日均成交额） | 132 | **+4.106pp** | 0.843 | 11/11 | 0.915 |

**退市 stub 敏感带**（几何超额，主口径）：`0.00` +3.086pp / `−0.30` +3.117pp / `−1.00` +3.191pp（跨档稳定）。

## ★强制并排披露（本方案不依赖它）

月度算术超额 t（简单 / Newey-West lag6）：主口径 **0.704 / 0.815**；G2 口径 **1.031 / 1.327**。
**均 < 2**——本方案的主判据是风险声明（σ 比），副判据是复利声明的 bootstrap P，
**不依赖均值的算术显著性**；此行原样披露，不回避也不包装。

## 硬门阈值（陈述值，独立于观测；★施加与裁定归 F007）

- 两个硬门几何超额门限 **+1.0pp**（沿用 B110 冻结的 +1.0pp 门限线，不新设）。
- 主判据 σ 比门限 **0.90**（来自复利拖累 ≈ σ²/2 的量级要求）；分年 **≥11/12**。
- 副判据 bootstrap **P(>0) ≥ 0.90**（副判据，故 0.90 非 0.95）。

## 诚实限制（§B.5，随产物）

无行业/规模中性、无停牌涨跌停可执行性、无交易成本（G2 之外）、无公司行动处置；
★**数据层可信度分层**：统计层已被 B110 两路零-import 独立复算证实，但数据层
（PIT `total_mv` 的 PIT 性、复权/退市终值、幸存者偏差）仍只有 B110 F004 单方证据；
★**窗口敏感性**：窗口冻结全区间，分段并排不用于挑选。
★**G2 流动性口径限制**：`amount` 取形成日**单日**（无前视但非滚动均值）——已知粗口径，
F007 若需更稳健可扩滚动窗（成本更高）。

---

**复现：** `python -m scripts.research.ashare_pit.low_vol_cli --liquidity data/research/B111/low_vol_liquidity.csv.gz`
