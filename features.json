{
  "sprint": "B031-llm-gateway",
  "features": [
    {
      "id": "F001",
      "title": "LLMGateway + routing + aigc-gateway HTTP client + secret 接线",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "(1) 新建 workbench_api/llm/__init__.py + gateway.py：ChatRequest / ChatResult dataclass frozen+slots；LLMGateway 类 + __init__ 接收 api_key + guard；advise(request) 路由 + cost guard check + httpx POST → ChatResult；embed(texts)；health_check() GET /balance；(2) 新建 workbench_api/llm/routing.py：ROUTING_TABLE 含 daily_advisor=claude-haiku-4-5 / quarterly_review=claude-sonnet-4-6 / quarterly_review_deep=claude-opus-4-7 / news_summarize=gemini-2.0-flash / topic_tagging=claude-haiku-4-5 / sharpe_tooltip=claude-haiku-4-5 / robinhood_simplify=claude-haiku-4-5 / embedding=cohere-embed-multilingual-v3 + fallback chain (_fallback_advisor/_fallback_news/_fallback_embedding) + _ci_mock；route_task(task) unknown → RuntimeError 含 routing.py 添加指引；(3) **永久边界 (l) 守门：业务代码不可硬编码 model name**（grep regression test 业务代码 0 命中 model name 字符串）；(4) 改 workbench_api/config.py：加 AIGC_GATEWAY_API_KEY 读取 (缺时 raise RuntimeError 含修复指引 'mcp__aigc-gateway__create_api_key or dashboard URL')；(5) 改 .env.example：加 '# AIGC Gateway API key for B031+ LLM access' AIGC_GATEWAY_API_KEY=；(6) 改 workbench/deploy/scripts/deploy.sh：加 pre-flight check (v0.9.25 §12.5 模式 + v0.9.30 §12.9 反 anti-pattern 规避)；(7) **改 .github/workflows/bootstrap-env.yml**（**v0.9.30 §12.9 关键铁律 — 不可漏！**）：加 AIGC_GATEWAY_API_KEY=${{ secrets.AIGC_GATEWAY_API_KEY }} 写入 production VM /etc/workbench/workbench.env；(8) 新建 workbench_api/llm/fixtures/aigc_gateway_chat_responses/ + aigc_gateway_balance_response.json mock 抽样响应；(9) pytest ≥12 测试：ChatRequest/ChatResult frozen+slots / LLMGateway 缺 API key RuntimeError / mock httpx POST /chat 解析 / mock 5xx 3 次重试 / mock 429 重试+backoff / health_check mock /balance 200 / route_task('daily_advisor') → claude-haiku-4-5 / route_task('unknown') → RuntimeError / 业务代码 grep 守门 model name 仅命中 routing.py / ROUTING_TABLE 含全部 task / 永久边界 (l) 守门 / API key 不入 build artifact；(10) Gates: pytest tests ≥408 baseline (B030) + ≥12 = ≥420 / ruff exit=0 / mypy exit=0 / frontend 不破 vitest ≥172；(11) 不动 B027/B028/B029/B030 既有 loader / scripts / strategy / trade/data/loader.py / workbench backend endpoints / Frontend UI / B026 banner decommissioned 状态。"
    },
    {
      "id": "F002",
      "title": "LLM cost guard + budget log + alembic migration",
      "priority": "high",
      "executor": "generator",
      "status": "pending",
      "acceptance": "(1) 新建 workbench_api/llm/cost_guard.py：MonthlyBudgetGuard dataclass (cap=200.0 USD ≈ ¥1500 / alert=0.80 / halt=1.00)；check_and_increment(estimated_cost_usd) 检查 + 写 budget log；BudgetExceeded exception 含修复指引 + ¥-equivalent 显示；(2) 新建 workbench_api/repositories/llm_budget_log.py：SQLite 表 llm_budget_log (date PK / month_year TEXT / call_count INT / total_cost_usd_est REAL)；CRUD get_month_total_usd(date) / increment(date, cost_usd)；(3) alembic migration add_llm_budget_log_table.py 参考 B027 既有 add_tiingo_budget_log_table.py 模式；不破既有 schema；(4) 改 LLMGateway.advise：入口调 guard.check_and_increment(estimated_cost) + 估算 cost (按 routing table 选定 model 的输入 token × 价格表估算)；(5) 改 /api/debug/recent-errors (B022 既有) 确认能捕获 llm_budget_near_cap warning + BudgetExceeded raise log；(6) pytest ≥10 测试：MonthlyBudgetGuard.default() (cap=200.0/alert=0.80) / check_and_increment(estimated=10) 月内首次通过 / 接近 80% (≥160 USD) log warning / ≥100% (≥200 USD) raise BudgetExceeded / BudgetExceeded 异常含 ¥-equivalent / llm_budget_log SQLite CRUD / 月份切换 5→6 归零 / alembic up/down 在 fixture DB / LLMGateway 集成 cost_guard (mock check_and_increment → advise 成功 / BudgetExceeded → 不调 HTTP) / llm_budget_log 表 schema 一致性；(7) Gates: pytest tests ≥420 + ≥10 = ≥430 / ruff + mypy 清 / alembic up/down 在 fixture DB 验证 / Frontend 不动；(8) 不动 F001 已完成的 gateway / routing 接口 / 其他 backend endpoint / strategy 代码。"
    },
    {
      "id": "F003",
      "title": "Codex L1 + L2 真 VM 验收 + signoff",
      "priority": "high",
      "executor": "codex",
      "status": "pending",
      "acceptance": "L1 (CI 内): F001 + F002 全 generator 验收脚本跑通 — backend pytest ≥430 / ruff / mypy / alembic up-down OK；Frontend 不动既有 vitest ≥172 + Playwright ≥38 不破；safety regression 全绿 (含 v0.9.29 §12.8.1 + **v0.9.30 §12.9 bootstrap-env.yml grep 守门确认 AIGC_GATEWAY_API_KEY 在 bootstrap-env.yml**) + **永久边界 (l) 守门**（业务代码 grep 无硬编码 model name）；artifact grep AIGC_GATEWAY_API_KEY value / aigc-gateway HTTP endpoint 字面值 0 命中；CI 完全离线 pytest --no-network 全过 (mock fixture)。L2 (真 VM): (1) Generator 实施期间已在 GitHub repo Settings 加 AIGC_GATEWAY_API_KEY secret (用户协助通过 mcp__aigc-gateway__create_api_key 生成)；deploy workflow inject 成功；(2) Production VM cat /etc/workbench/workbench.env | grep AIGC_GATEWAY_API_KEY 存在 (值脱敏)；(3) curl https://trade.guangai.ai/api/health 200 + version SHA 与 main HEAD 等价；(4) curl https://trade.guangai.ai/api/debug/recent-errors 返回 {\"count\":0,\"records\":[]}；(5) Production backend log (systemd journal) 含 alembic migration add_llm_budget_log_table 成功记录；(6) SSH 到 VM 跑 sqlite3 ... 'SELECT name FROM sqlite_master WHERE type=table AND name=llm_budget_log' 返回 1 行；(7) smoke API test (Evaluator 临时跑一次)：production backend SSH 触发 LLMGateway().health_check() → aigc-gateway /balance 返回 200 / llm_budget_log 不应增 (health check 不计 cost)；(8) **B026 banner 仍 decommissioned 不破** (HTML grep '研究原型' / 'SyntheticDataBanner' 0 hits)；(9) Production HEAD ≡ main HEAD (v0.9.25 §Production/HEAD 等价性) + Post-signoff Deploy 段 (v0.9.27 / §12.7.1)。Signoff: docs/test-reports/B031-llm-gateway-signoff-2026-MM-DD.md 用 framework/templates/signoff-report.md (含 v0.9.27 §Post-signoff Deploy + **v0.9.31 §Decommission Checklist '本批次不含 decommission'** + §Production/HEAD)；docs/screenshots/B031-llm-gateway/ ≥2 PNG (production /api/health + llm_budget_log 表存在)。Framework 候选：预期无重大；若 fix-round 出现 aigc-gateway 接入意外 (鉴权 / rate limit / 价格表估算偏差 / MCP vs HTTP 混淆) 记录 signoff §Framework Learnings。更新 progress.json status→done / docs.signoff / evaluator_feedback。"
    }
  ]
}
